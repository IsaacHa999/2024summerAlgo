// boj 117118   문자열 길이 구하기
#pragma GCC optimize("O3")

#include <bits/stdc++.h>

#define endl '\n'
#define ll long long
using namespace std;

// 함수 선언

// 전역 변수

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // 변수 선언, 초기화
    while (true)
    {
        string str;
        getline(cin, str);
        cout << str << endl;
        if (str == "")
            break;

    }

    // 로직
}

// 함수 정의